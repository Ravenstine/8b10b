/**
 * @typedef {1 | -1} RunningDisparity
 */

/**
 * @typedef 8b10bState
 * @property {?Number} word - The decoded 8b word returned from the function
 * @property {?Number} symbol - A 10b symbol in the form of an integer
 * @property {RunningDisparity} runningDisparity - The current running disparity
 */
